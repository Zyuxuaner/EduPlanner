spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/tp5?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC"
    username: "root"
    password: "yunzhi"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQL57Dialect"
    hibernate:
      ddl-auto: create-drop
    show-sql: true